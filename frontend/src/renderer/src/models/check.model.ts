
/**
 * Represents a model for version checks.
 * 
 * @interface CheckModel
 * @property {string} version - The version string of the application
 */
export interface CheckModel {
  version: string;
}
