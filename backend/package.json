{
  "name": "backendserver",
  "version": "1.0.0",
  "description": "Development server for the backend",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "cd api && php -S localhost:8000 routes.php"
  },
  "dependencies": {
    "php-server": "^2.0.0"
  }
}
